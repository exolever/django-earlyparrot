============
Installation
============

At the command line::

    $ easy_install exolever/django-earlyparrot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv exolever/django-earlyparrot
    $ pip install exolever/django-earlyparrot
